services:
  erfassung:
    image: ghcr.io/OWNER/erfassung:0.2.0
    container_name: erfassung
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:////app/data/erfassung.db
      # SESSION_SECRET_KEY: change-me-in-production
      # HTTPS_ONLY_SESSION: "true"   # Setze auf true wenn hinter HTTPS-Proxy (z.B. Traefik)
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./config:/app/config
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/login', timeout=5)"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s
    restart: unless-stopped
